package Constants;

public enum Assembly {
    itr, // interrupt
    add, // add
    sub, // subtract
    mul, // multiply
    div, // divide
    mov, // move

    cmp, // compare
    jgt,  // jump if greater than
    jlt, // jump if less than
    jmp, // jump

    hlt, // halt
    lbl, // label
    end, // end

}
